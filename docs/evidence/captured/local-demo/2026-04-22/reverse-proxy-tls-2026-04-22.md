# Reverse Proxy and TLS Evidence

- Environment: `local-demo`
- Capture date (UTC): 2026-04-22
- Operator: Codex local run

## Routing Map Captured

The only proxy captured in this pass is the temporary local demo proxy generated by `scripts/demo-agency-flow.sh`.

| Public local path | Internal service |
| --- | --- |
| `http://localhost:8090/public/gtfs/*` | `http://localhost:8081` |
| `http://localhost:8090/public/feeds.json` | `http://localhost:8081` |
| `http://localhost:8090/public/gtfsrt/vehicle_positions.pb` | `http://localhost:8083` |
| `http://localhost:8090/public/gtfsrt/trip_updates.pb` | `http://localhost:8084` |
| `http://localhost:8090/public/gtfsrt/alerts.pb` | `http://localhost:8085` |

This proves the repo demo can place public feed paths behind one local root. It does not prove hosted reverse proxy readiness.

## TLS Certificate / Renewal

- TLS certificate: Missing.
- Certificate issuer: Missing.
- Validity window: Missing.
- Renewal posture: Missing.
- Renewal logs/checks: Missing.

Blocker: the environment used for this packet has no public hostname, certificate, load balancer, ingress, Caddy, Nginx, Traefik, Certbot, or equivalent TLS termination artifact.

## HTTP-to-HTTPS Redirect

- Not applicable in `local-demo`.
- No port 80 or HTTPS edge was configured.
- Redirect behavior remains missing deployment evidence.

## Protected Admin/Debug Boundary

Local anonymous access checks returned 401 for admin/debug surfaces:

- `/admin/compliance/scorecard`
- `/v1/events?limit=10`
- `/public/gtfsrt/vehicle_positions.json`
- `/public/gtfsrt/trip_updates.json`
- `/public/gtfsrt/alerts.json`
- `/admin/gtfs-studio`

Supporting service request logs are stored under `artifacts/logs/`.

## Conclusion

Reverse proxy routing is partially proven for a local loopback demo only. TLS, redirect, certificate renewal, and hosted admin/debug network boundary evidence remain missing.
