# Phase Handoff Template

All future phase handoff files must use this structure unless the phase explicitly documents a reason to diverge.

## Phase

Phase 9 — Production Closure

## Status

- Complete for the current codebase surface
- Active phase after this handoff: Phase 10 — Docs, Tutorials, Deployment, and Demo

## What Was Implemented

- Tightened `/admin/validation/run` so requests may provide only `validator_id`, `feed_type`, and optional `feed_version_id`; command/path/argv/output/artifact fields and `agency_id` are rejected as unknown fields.
- Schedule validation derives ZIP bytes server-side from the schedule builder and records the feed version used.
- Realtime validation prefers internal builder-derived Vehicle Positions, Trip Updates, and Alerts protobuf bytes; configured feed URLs remain fallback only when internal builders cannot be constructed.
- Validator execution remains allowlisted, argv-based, timeout-bounded, stdout/stderr/report-capped, temp-directory confined, and redacted in stored argv/path reporting.
- Added repo-supported pinned validator tooling with `tools/validators/validators.lock.json`, `make validators-install`, `make validators-check`, and Task equivalents.
- Added structured request logs, `X-Request-ID`, secret/query redaction rules, and `/metrics` only when `METRICS_ENABLED=true`.
- Tightened `/readyz` for `agency-config`, Trip Updates, and Alerts so readiness requires DB reachability plus required active feed/config dependencies instead of DB ping alone.
- Strengthened telemetry device-token closure tests for spoof rejection, rebinding, immediate old-token invalidation, new-token verification, and audit logging.
- Strengthened assignment race coverage with a partial unique-index assertion and higher-concurrency current-row writes.

## What Was Designed But Intentionally Not Implemented Yet

- Full hosted login/SSO and server-side admin JWT `jti` replay tracking.
- Production SLO dashboards, alerting, and Prometheus/Grafana deployment assets beyond the internal `/metrics` endpoint and request logs.
- Optional external prediction backends such as TheTransitClock.
- Phase 10 README/tutorial/demo/docs-asset polish.
- Phase 11 compliance evidence packaging and external-consumer acceptance claims.

## Schema And Interface Changes

- No database migration was added.
- `/admin/validation/run` request shape is now strictly `validator_id`, `feed_type`, and optional `feed_version_id`.
- `schedule.Builder` now supports `SnapshotForFeedVersion` for server-side validator artifact derivation.
- `ValidationRunInput` records `RealtimeArtifactSource` internally so stored reports can distinguish internal builder artifacts from feed-URL fallback.
- `X-Request-ID` is returned on HTTP responses from services run through `internal/server`.
- `/metrics` is registered only when `METRICS_ENABLED=true`.
- `schedule.Builder` and Trip Updates builder now expose explicit readiness checks for their active feed dependency; Alerts readiness checks active feed through the GTFS repository at the service boundary.

## Dependency Changes

- Added pinned static GTFS validator artifact:
  - MobilityData GTFS Validator `v7.1.0`
  - SHA-256 `52c2785089aaf04e7ba1bb11b2db215692e2622eb0e196b823c194d156d9b58c`
- Added pinned GTFS-RT validator image:
  - `ghcr.io/mobilitydata/gtfs-realtime-validator@sha256:5d2a3c14fba49983e1968c4a715e8ca624d4062bf4afede74aeca26322436c89`
- Repo-supported GTFS-RT execution uses `.cache/validators/gtfs-rt-validator-wrapper.sh`, a Docker-backed wrapper generated by `make validators-install`.
- Direct non-Docker GTFS-RT validator executables are still runtime-capable, but are not accepted by `make validators-check` as pinned proof unless a future phase adds an equivalent checksum/digest contract.
- `VALIDATOR_TOOLING_MODE=stub` is the explicit deterministic stub bypass for targeted tests.

## Migrations Added

- None.

## Tests Added And Results

- Added handler tests proving schedule and realtime validator runs return `200`, persist validation results, normalize status, and record feed type/feed version.
- Added unknown-field validation tests for `/admin/validation/run`.
- Added agency-config protection tests proving admin validation rejects unauthenticated requests while public schedule ZIP remains anonymous.
- Added DB-backed device rebind tests proving spoofed bindings fail, old tokens stop working immediately, new tokens work, and audit rows are written.
- Strengthened DB-backed assignment concurrency tests and partial-index assertions.
- Added server middleware tests for request IDs, redacted query logging behavior, and `/metrics` gating.
- Added readiness tests proving DB-unavailable, missing active feed/config dependency, and satisfied-dependency outcomes for `agency-config`, Trip Updates, and Alerts.

## Checks Run And Blocked Checks

- `gofmt -w ./cmd ./internal`: passed.
- `go mod tidy`: passed.
- `go test ./...`: passed.
- `make validators-install`: passed.
- `make validators-check`: passed.
- `make validate`: passed.
- `make test-integration`: passed.
- `make smoke`: passed.
- `docker compose -f deploy/docker-compose.yml config`: passed.
- `git diff --check`: passed.
- Blocked commands: none.

## Known Issues

- `make smoke` now verifies pinned tooling and hardening packages, but it is still not a fully scripted multi-process demo. Phase 10 should document a runnable agency demo flow without overstating it as consumer acceptance.
- The GTFS-RT Docker wrapper is the repo-supported pinned path. Deployments that want a native executable must add their own equivalent pinning/checksum evidence before making stronger validation-readiness claims.
- `/metrics` is intentionally an internal operations surface; deployment docs must place it behind reverse proxy/network controls.

## Exact Next-Step Recommendation

- First files to read:
  - `docs/current-status.md`
  - `docs/handoffs/latest.md`
  - `docs/phase-plan-production-closure.md`
  - `docs/prompts/calitp-truthfulness.md`
- First files likely to edit:
  - `README.md`
  - `docs/tutorials/local-quickstart.md`
  - `docs/tutorials/deploy-with-docker-compose.md`
  - `docs/tutorials/agency-demo-flow.md`
  - `docs/tutorials/production-checklist.md`
  - `docs/tutorials/calitp-readiness-checklist.md`
  - `docs/assets/README.md`
- Commands to run before coding:
  - `make validators-install`
  - `make validators-check`
  - `make test`
  - `make smoke`
- Known blockers:
  - Docker must be running for pinned GTFS-RT validator image checks and DB-backed integration tests.
  - Task remains optional; Makefile is independently usable.
- Recommended first implementation slice:
  - Rewrite README and local quickstart to match Phase 9 exactly, including pinned validator setup, admin JWT generation, device-token telemetry ingest, anonymous protobuf feeds, protected debug/admin routes, and truthful compliance wording.
